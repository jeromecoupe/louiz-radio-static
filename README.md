# Louiz Radio: static templates

Static templates for Louiz radio. Powered by 11ty / NPM scripts / esbuild.

## Installation

- run `npm install`
- run `npm run build` to build the project
- run `npm run dev` to spin a local server
